// API configuration

const API_CONFIG = {
  BASE_URL: '', // Empty base URL to use the proxy
  ENDPOINTS: {
    VESSELS: '/api/vessels',
    CREW: '/api/crew',
    VOYAGES: '/api/voyages',
    MAINTENANCE: '/api/maintenance'
  }
};

export default API_CONFIG;
